<% content_for :body do %>
  <ul id="games">
    <% @games.each do |game| %>
      <li>
        <%= link_to game.name, refinery.style_game_path(game) %>
      </li>
    <% end %>
  </ul>
<% end %>

<%= render '/refinery/content_page' %>
